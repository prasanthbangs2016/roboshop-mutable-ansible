- name: Copy file beat yum repo
  ansible.builtin.template:
    src: filebeat.repo
    dest: /etc/yum.repos.d/filebeat.repo

- name: Install filebeat
  ansible.builtin.yum:
    name: filebeat
    state: installed


  

